# handoff-2026-05-03b.md

**Path:** docs/design/handoff-2026-05-03b.md
**Syntax:** markdown
**Generated:** 2026-05-03 21:07:47

```markdown
# Sr. Barbara's Class — Session Handoff
**Date:** 2026-05-03 (evening session)
**Project repo:** https://github.com/carolynboyle/sr-barbara
**Local repo:** ~/projects/sr-barbara (MX Linux daily drive)

---

## What this project is

A web-based Reed-Kellogg sentence diagramming game. Flask backend,
PostgreSQL database, vanilla JS + SVG frontend, Docker containerized.
Current goal: ship a single-file HTML build that anyone can download
and play with no installation.

---

## What was accomplished this session

### 1. `build_game.py` — single-file HTML build pipeline

Changeset doc: `docs/changedocs/sr-barbara-build-game.md`

**`scripts/src/sr_barbara_scripts/build_game.py`** — new file (replaces
empty `build_slingcode.py` placeholder). Three classes:

- `ConfigRenderer` — reads `data/game_config.yaml` and `pyproject.toml`,
  renders `config.js` content as a string. Injects `VERSION` and
  `GITHUB_REPO` constants for the update checker.
- `DataRenderer` — reads `data/sentences.yaml`, flattens phrase/token
  structure, synthesizes sequential token IDs, renders
  `data.standalone.js` with a local `fetchSentence()` that replaces
  the Flask API call.
- `GameBuilder` — orchestrates the build. Reads
  `templates/index.standalone.html` as template, inlines CSS, inlines
  all JS modules in correct load order, base64-encodes
  `static/images/sr_barbara.png`, writes `dist/sr_barbara.html`.

**`templates/index.standalone.html`** — new file. Same structure as
`index.html` but no `url_for()` calls. Three placeholders:
`<!-- INLINE_CSS -->`, `<!-- INLINE_SCRIPTS -->`, `{{ IMAGE_SRC }}`.

**`sr_barbara.yaml`** — one path entry added:
`standalone_template: templates/index.standalone.html`

**`pyproject.toml`** — entry point updated:
`srb-build = "sr_barbara_scripts.build_game:main"`

**Install command:** `pip install -e .` (run from repo root, not
`scripts/` — `pyproject.toml` is at root).

**Output:** `dist/sr_barbara.html` (~1.2MB, dominated by base64 image).
Opens in any browser with no server.

### 2. GitHub Actions release pipeline

Changeset doc: `docs/changedocs/sr-barbara-release-workflow.md`

**`.github/workflows/release.yml`** — new file. Triggers on push to
`main` when `pyproject.toml` changes (version bump = release signal).
Installs Python, runs `srb-build`, reads version via `tomllib`, creates
a GitHub Release tagged `v{version}` with `sr_barbara.html` as the
asset.

First release cut: **v0.1.1** — visible at
https://github.com/carolynboyle/sr-barbara/releases

### 3. Update checker and help overlay

Changeset doc: `docs/changedocs/sr-barbara-update-checker.md`

**`static/js/ui.js`** — four functions appended:
- `checkForUpdate()` — hits GitHub Releases API on page load, shows
  lightbulb if a newer version exists. Fails silently if offline.
- `isNewerVersion(latest, current)` — semver comparison
- `showHelp()` / `hideHelp()` — help overlay open/close

**`static/js/main.js`** — updated:
- Added DOM refs: `updateBtn`, `updateNotice`, `helpBtn`, `helpOverlay`
- Added event listeners for lightbulb toggle, help open/close, backdrop
  click to close
- `checkForUpdate()` called at end of init
- **Critical fix:** replaced `DOMContentLoaded` listener pattern with
  `init()` function + bootstrap block:
  ```javascript
  if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', init);
  } else {
      init();
  }
  ```
  This is required because in the standalone built file, scripts are
  inline and `DOMContentLoaded` has already fired by the time they run.
  The Flask-served version still works correctly.

**`static/css/style.css`** — appended styles for `#icon-bar`,
`#update-indicator`, `#update-btn`, `#update-notice`, `#help-btn`,
`#help-overlay`, `#help-panel`, `#help-close`. Also added
`!important` to `.hidden` rule to fix specificity conflict with
`#help-overlay`'s `display: flex`.

**`templates/index.html` and `templates/index.standalone.html`** —
both updated:
- `#icon-bar` added below Sr. Barbara image: lightbulb (hidden by
  default) stacked above ❔ help button
- `#help-overlay` added as direct child of `<body>` (outside `<main>`
  — required for `position: fixed` to work correctly)
- `#solve` button gets `class="hidden"` — starts hidden, revealed by
  `initDiagram()` when a sentence loads

**`#sidebar`** — changed `align-items` from `center` to `flex-start`
so icons sit to the left of Sr. Barbara rather than centered under her.

---

## Current verified state

- `srb-build` runs cleanly, reports version and file size
- `dist/sr_barbara.html` opens in Firefox, LibreWolf, and Falkon
- Game plays correctly in standalone build (no server)
- ❔ opens chalkboard help overlay; X and backdrop click close it
- 💡 appears when `VERSION` is older than latest GitHub Release
- Lightbulb click shows inline update notice with correct release URL
- Solve button hidden until sentence loads
- GitHub Actions pipeline runs on version bump, cuts release with asset
- Docker/Flask version unaffected — `index.html` serves correctly
- v0.1.1 release live at GitHub Releases with `sr_barbara.html` asset

---

## Immediate next steps (in order)

1. **Write help content** — `#help-panel` currently shows "Help goes
   here." placeholder. Write actual how-to-play instructions. Update
   both `index.html` and `index.standalone.html`, then `srb-build`.

2. **Cut v0.1.2** — bump version in `pyproject.toml`, commit, push.
   Pipeline will build and release automatically. Verify update checker
   fires in previous build.

3. **CSS cleanup** — `style.css` still has a `pos-article` rule at the
   bottom (pre-determiner leftover). Remove it.

4. **Prepositional phrase chaining** — roadmap item, medium priority.
   Requires `modifies_part_id` column in `sentence_parts`. Design
   before implementing.

5. **Subordinate clause support** — known complexity threshold for
   schema rethink. Design before implementing.

---

## Known issues / noted for later

- **`config.js` still exists as a static file** and must be kept in
  sync with `game_config.yaml` by hand for the Flask-served version.
  The standalone build always generates fresh from YAML. Long-term fix:
  make Flask serve a generated `config.js` from YAML too, or just
  accept the manual sync for development.

- **`character.name` not yet effective** — "Sr. Barbara" is hardcoded
  in both HTML templates. Needs a template pass before `character.name`
  in config does anything.

- **`completed.png` and `start.png`** in `static/images/` are Dev.to
  submission assets, not used by the game. Safe to ignore; could be
  moved to `docs/` eventually.

- **Settings/profile panel** — roadmap. Discussed possibility of
  GitHub Discussions as backend for player profiles/login. Deferred.

---

## Key files to attach to new conversation

- `project_rules.md` (always)
- `manifest.fletch` (repo file list with raw URLs)
- `handoff-2026-05-03b.md` (this file)

```
